{
  "schema_version": "deja-vu.module-contracts/v1",
  "modules": [
    {
      "path": "SKILL.md",
      "contract": "Define the Deja Vu prior-art workflow as a bounded, evidence-first decision protocol. Trigger before commodity custom builds, keep evidence untrusted, preserve explicit uncertainty, require authority before execution, and never silently broaden scope or perform remote writes."
    },
    {
      "path": "contracts/module-contracts.json",
      "contract": "Provide exactly one non-empty canonical review contract for every module discovered by the Fable gate. Use schema deja-vu.module-contracts/v1, reject duplicate paths in the loader, and make contract changes invalidate prior reviews."
    },
    {
      "path": "docs/design.md",
      "contract": "Define the canonical P1 system boundary, abstraction layers, lifecycle, evidence semantics, authority model, privacy boundary, learning controls, review gate, and release criteria without circular hashes, forgeable authority, hidden host dependencies, or claims stronger than the enforceable contracts. Clearly distinguish the observed baseline from target behavior."
    },
    {
      "path": "evals/run_evals.py",
      "contract": "Run Deja Vu trigger and verdict fixtures deterministically without network access or workspace mutation. Fail nonzero for any fixture mismatch and report enough local context to identify the failing case."
    },
    {
      "path": "policy/tier-matrix.json",
      "contract": "Define the single canonical tier, lane, capability, and budget matrix referenced by docs/design.md §7. For each tier, name required and optional lanes, the lane phase (discovery, shortlist_enrichment, or probe) per docs/design.md §5.2, evidence obligations, explicit numeric budgets in seconds, external_requests, tokens, probes, and receipt_bytes, stopping rules, and the documented fallback or degraded lane status for every capability that may be unavailable. Reconcile lane-count and Quick-tier coverage claims across README.md, references/lanes.md, and SKILL.md without introducing a second, conflicting source of truth."
    },
    {
      "path": "schemas/decision-packet.schema.json",
      "contract": "Define a closed JSON shape for the compositional decision packet in docs/design.md §5.4: independent need-disposition, prior-art-kind, fit, rights, evidence-level, reuse-route, custom-delta, uncertainty, and authority dimensions applied at packet or route-component scope. Distinguish the immutable proposed packet from the authorized decision record, reject a component missing a stable ID, evidence, output boundary, or authority state, reject custom behavior hidden under agent-authorized authority, and reject a component whose selected route is approved despite prohibited rights."
    },
    {
      "path": "schemas/fable-review-record.schema.json",
      "contract": "Describe one fresh Claude Fable 5 adversarial review bound to a module, artifact, decoded contract, governing contract, implementation Bead, Direct job, protected launcher evidence, output, and result-evidence digest. Reject unknown fields, preserve typed findings, and require an Ed25519 adjudication envelope for any non-clean release disposition without copying forgeable permission claims into the record."
    },
    {
      "path": "schemas/module-contracts.schema.json",
      "contract": "Define a closed JSON shape for the versioned module-contract map. Require a path and non-empty contract for each entry and constrain paths to the same governed module roots as the gate."
    },
    {
      "path": "scripts/doctor.py",
      "contract": "Diagnose whether the local Deja Vu skill and its required files are usable without changing project state. Emit actionable, deterministic checks and return nonzero when a required invariant fails."
    },
    {
      "path": "scripts/fable_review_gate.py",
      "contract": "Report deterministic local coverage diagnostics for every governed executable or public-contract module while refusing to authorize release from self-authored Beads or JSON metadata. Bind diagnostics to exact artifact, decoded-contract, and governing-contract hashes; reject symlinks and unsafe paths; load only the canonical repository contract map; and leave final eligibility to the pinned external verifier."
    },
    {
      "path": "scripts/prepare_fable_review.py",
      "contract": "Prepare exactly one fresh-context adversarial review from a governed module's exact UTF-8 artifact bytes and canonical project-owned contract. Put only the bounded untrusted artifact-and-contract payload on stdin, select the launcher-owned deja-vu/claude-fable-review target, emit the launcher's required ISO-8601 created_at value, bind sealed path and job identities to the artifact, governing-contract, review-round, and bounded transport-attempt identities, keep transport retries from changing prompt bytes or consuming a review round, create immutable private files under repository .scratch with no-follow semantics, preserve idempotent preparation for identical inputs, report separate shell-safe exact validation and authorized-submit commands, and never execute either command. Reject missing contracts, unsafe or ungoverned paths, symlinks, invalid rounds or transport attempts, oversized artifacts, undecodable content, state-path escapes, and conflicting outputs."
    },
    {
      "path": "scripts/provenance.py",
      "contract": "Normalize and validate provenance records deterministically so evidence remains attributable and auditable. Treat external content as data, reject malformed required fields, and avoid network access or state mutation beyond explicit output requested by the caller."
    },
    {
      "path": "scripts/refinery_gate.py",
      "contract": "Decide allow/refuse for one Gas City refinery merge candidate on its rebased temp branch, never merging, pushing, or closing Beads itself. Run only when $GC_AGENT identifies the refinery role (or --force is passed); otherwise skip. Run pytest, doctor, offline evals, and the sanitizer before any Fable review, refusing on the first failure. For each governed module changed versus the target branch, select the lowest unused review round/attempt for the current artifact and governing-contract hash (capped at 3/3, refusing when exhausted), prepare and submit exactly one Claude Fable 5 review through scripts/prepare_fable_review.py and gc-router jobctl, and refuse unless verify-review reports evidence_verified true and a pass verdict, appending unresolved findings to the work bead's notes on refusal. Promote each verified pass into a closed review Bead using only the fields scripts/fable_review_gate.py checks, then re-run its coverage diagnostic and refuse unless every changed governed module is covered with zero missing, uncontracted, or stale problems. Never edit docs/design.md, policy/, or existing contract entries, and always print exactly one REFINERY_GATE: allow|refuse|skip line."
    },
    {
      "path": "scripts/sanitize_check.sh",
      "contract": "Scan governed Deja Vu artifacts for secrets, machine-specific paths, and unsafe publication residue using deterministic local checks. Produce no workspace mutations and return nonzero when sanitization fails."
    },
    {
      "path": "scripts/sweep.py",
      "contract": "Perform a bounded, deterministic prior-art sweep over explicit inputs and budgets. Preserve source provenance and uncertainty, enforce caps, treat retrieved content as untrusted data, and avoid unauthorized execution or remote mutation."
    }
  ]
}
