{"prompt": "Let's build a rate limiter for our API gateway.", "expected": "fire", "why": "explicit build ask for a commodity rate limiter named in SKILL.md's trigger list", "matched_family": "rate limiter"}
{"prompt": "We need webhook delivery for the new integrations partner.", "expected": "fire", "why": "\"we need a\" + commodity subsystem noun (webhook delivery)", "matched_family": "webhook delivery"}
{"prompt": "I'll write a CSV dedupe script to clean up the nightly export before it loads.", "expected": "fire", "why": "\"I'll write a script that\" phrasing plus a dedupe-engine-shaped ask", "matched_family": "dedupe engine"}
{"prompt": "Can you implement caching for this API so we stop hammering the DB on every request?", "expected": "fire", "why": "\"implement a\" + cache, a named commodity subsystem", "matched_family": "cache"}
{"prompt": "I think we should roll our own auth/SSO flow instead of paying for Auth0.", "expected": "fire", "why": "\"should I roll my own or use a library\" framing around auth/SSO, a named commodity subsystem", "matched_family": "auth/SSO flow"}
{"prompt": "Let's build a job queue to handle background email sends.", "expected": "fire", "why": "\"let's build\" + job queue, a named commodity subsystem", "matched_family": "job queue"}
{"prompt": "I'll write a small parser for this config format we invented.", "expected": "fire", "why": "\"I'll write a\" + parser, a named commodity subsystem", "matched_family": "parser"}
{"prompt": "We need a retry/backoff layer for these flaky HTTP calls to the vendor.", "expected": "fire", "why": "\"we need a\" + retry/backoff logic, a named commodity subsystem", "matched_family": "retry/backoff logic"}
{"prompt": "Let's create a diff engine to compare these two JSON blobs field by field.", "expected": "fire", "why": "\"create a subsystem/service for\" + diff engine, a named commodity subsystem", "matched_family": "diff engine"}
{"prompt": "I want to implement a template engine for our report generator instead of string concatenation.", "expected": "fire", "why": "\"implement a\" + template engine, a named commodity subsystem", "matched_family": "template engine"}
{"prompt": "We need a config loader that supports YAML, JSON, and env-var overrides.", "expected": "fire", "why": "\"we need a\" + config loader, a named commodity subsystem", "matched_family": "config loader"}
{"prompt": "Time to build a migration runner for these database schema changes.", "expected": "fire", "why": "\"let's build\"-equivalent + migration runner, a named commodity subsystem", "matched_family": "migration runner"}
{"prompt": "Let's wire up an event bus so services can publish and subscribe without direct calls.", "expected": "fire", "why": "new-subsystem proposal for an event bus, a named commodity subsystem", "matched_family": "event bus"}
{"prompt": "We should build a feature-flag system in-house rather than adopt one.", "expected": "fire", "why": "\"should I roll my own\" framing + feature-flag system, a named commodity subsystem", "matched_family": "feature-flag system"}
{"prompt": "Is there something that already does distributed locking, or should we roll it ourselves? Don't want to reinvent the wheel here.", "expected": "fire", "why": "direct \"is there something that already does X\" + \"don't reinvent the wheel\" trigger phrases", "matched_family": "don't reinvent the wheel"}
{"prompt": "Fix the typo in this docstring — 'recieve' should be 'receive'.", "expected": "silent", "why": "pure typo fix, no new subsystem or design moment", "matched_family": null}
{"prompt": "Rename the `getUserData` function to `fetchUserData` across the file.", "expected": "silent", "why": "pure rename, nothing new is being constructed", "matched_family": null}
{"prompt": "Bump the request timeout config value from 30 seconds to 60 seconds.", "expected": "silent", "why": "one-line config tweak, not a build/design moment", "matched_family": null}
{"prompt": "Go ahead and continue implementing the rate limiter we already approved building last week.", "expected": "silent", "why": "continuing a build already approved by a prior deja-vu run; the decisions registry already covers it, so it should not re-fire", "matched_family": null}
{"prompt": "How does OAuth 2.0's authorization code flow work under the hood?", "expected": "silent", "why": "pure question seeking an explanation, not a request to build or design anything", "matched_family": null}
{"prompt": "What's the practical difference between TCP and UDP for a game server?", "expected": "silent", "why": "pure question, no build/design moment and no commodity-subsystem construction implied", "matched_family": null}
